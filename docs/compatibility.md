# Compatibility

## Initial targets

### Host and runtime

| Component | Target contract | Current evidence | Not yet proven |
|---|---|---|---|
| Windows host | Windows 10 22H2 build 19045 or newer, amd64, with either working WSL2 or firmware virtualization available for WSL2 enablement | Go build and tests on `windows-latest`; host evaluation treats an operational WSL2 runtime as authoritative when active Hyper-V hides the firmware flag. On build 19045, the local unsigned native setup resumed a real partial installation, displayed and persisted both storage locations, completed bootstrap, created the Start-menu shortcut through `IShellLinkW`, registered sign-in startup and uninstall, launched the installed daemon, and returned `status: ok` from `127.0.0.1:4444/health`. A source-built `0.1.2` daemon and GUI were then run in an isolated repository-owned home on `127.0.0.1:4455`; the native `LCTK` top-level window stayed alive, authenticated without a browser, and exposed the expected project, lifecycle, Codex, grants, logs, refresh, and uninstall controls. | A clean-host execution of the final unsigned setup through its Windows warning, WSL enablement and reboot, managed-machine creation, first project, and complete uninstall |
| macOS host | Development compatibility target only; no official package | Go build and tests on the native arm64 `macos-15` runner; the Stage 7 dry-run built, extracted, and executed the launcher/core archive and preserved its Darwin/arm64 identity JSON | An accepted runtime/distribution decision, execution on macOS 13, launch service, APFS edge cases, and end-to-end lifecycle evidence |
| Managed runtime | LCTK-private Podman `5.8.2`, explicit `lctk-runtime-root` connection, WSL2 machine `lctk-runtime` | Unit and transaction tests cover absolute client selection, explicit `XDG_DATA_HOME`, host-path translation, immutable downloads, safe and idempotent extraction, explicit network/volume/container lifecycle, runner isolation, and machine initialization arguments. A real Windows 10 run continued the managed machine after setup failure, pulled both signed OCI identities and the embedding model, passed the inference self-test, and repeated setup without replacing the running verified proxy. | Clean-host creation at a non-default runtime-data location, concurrent multi-project load, and runtime replacement/rollback |

### Client and capability

| Component | Target contract | Current evidence | Not yet proven |
|---|---|---|---|
| MCP | Streamable HTTP through official Go SDK v1.7.0 | In-process tests cover dispatch and scope. An independent Go SDK client listed and called the complete 18-tool catalog against the persistent production stack, including expected typed refusals and memory recovery across restart, then repeated all 18 calls against the final Stage 7 image. | Other SDK versions, concurrent client load, and OAuth-only clients |
| Codex client | Streamable HTTP, MCP protocol `2025-06-18`, with `url` plus `bearer_token_env_var` | Slice 0.4 and Slice 1.4 proved handshake, route/grant refusal, stopped-project behavior, and reconnect. Stage 7 then ran Codex CLI `0.146.0-alpha.9.2` as an agent against the real five-file stack and called all 18 tools, including the complete memory lifecycle and expected command/not-found refusals; every foreign `project_id` remained route-scoped. See [Stage 7 client verification](stage7-client-verification.md). | macOS, the extension UI itself, non-alpha Codex versions, timeout enforcement, repeated or concurrent sessions, and the experimental app-server protocol remaining stable |
| Second MCP client | Any client speaking Streamable HTTP with a bearer credential | Slice 1.4 ran the same chain by hand against Claude Code on the same live endpoint, including a real agent session calling `project_info` with a foreign `project_id` and receiving the routed project; the credential was stored as a variable reference and a missing variable was reported by name | Other clients, clients without environment-variable expansion in their configuration, and OAuth-only clients |
| Exact search | Persistent indexed literal and regular-expression search over the saved working tree | Slice 1.5 proved live indexing, mutation, restart reuse, and `git grep` parity. Stage 7 then published a real Docker-volume index from 1,000,000 ordinary bind-mounted files in 4,238.336 seconds and returned the unique literal match in 1,753.652 ms without truncation; see [stress evidence](stress.md). | Heterogeneous million-file repositories, concurrent query/index load, the million-file curve on arm64, and index behaviour across an engine upgrade |
| Semantic search | Persistent hybrid retrieval over AST-aware chunks, with one shared local embedding process and isolated per-project state | Stage 5 proved conceptual and exact-identifier queries through live MCP, persistence, batching, failure atomicity, and two projects sharing inference without sharing state. Stage 7 measured the production schema and exact-vector adapter through one million chunks with bounded ranking memory; see [stress evidence](stress.md). | Concurrent query/index load, heterogeneous million-file code, alternative vector adapters, and macOS Docker Desktop |
| Incremental indexing | A saved file reaches the search index without any command, and a search sees an edit made a moment earlier | Slice 2.2 ran the whole loop against this repository through a real daemon, a real container, and the live endpoint: a daemon start reconciled 172 files and closed its gap, a file written was found by search 0.2 s later against a 3 s debounce window, a deletion left the index one generation later, a removed directory took both of its files with it, and a file written while the daemon was down was picked up by the reconciliation on the next start | Bulk escalation on a real project large enough to reach the 500-file floor, sustained editing over hours, several projects updating at once, and behaviour when the index is slower than the edit rate |
| Command runner | Approved commands only, in a container with one writable mount, capped processes, memory and CPU, an explicit network policy, and a timeout | Slice 3.2 ran a real approved command in a real container against this repository and probed the guardrails from inside it: `pids.max` 512, `memory.max` 2 GiB, `cpu.max` 2 cores, `/workspace` writable and the only mount, no Docker socket, no name resolution under `none` and resolution under `full`. A command rewritten in the manifest was refused live and accepted again after re-approval; refusals and runs both reached the audit log | A command that ignores SIGTERM, very long builds, concurrent runs in one project, images without `/bin/sh`, and the re-download cost of having no cache mount |
| Git awareness | Branch, commit, dirty state, changed files, and a bounded diff, read-only and route-scoped | Slice 3.1 drove the tools live against this repository through the endpoint: all four tools listed, `project_info` carrying branch and commit, six changed paths reported with a deliberately wrong `project_id` argument, a 58-line single-file diff, a diff bounded to 80 bytes and reported truncated, and absolute, escaping, and dash-leading paths refused. The parser is tested against real repositories the tests create, on Git 2.54 | Older Git versions, worktrees and submodules, repositories on network or case-insensitive volumes, and very large working trees where status is slow |
| Filesystem watching | Native events on Windows and macOS, normalized to project-relative changes, with every lapse in observation recorded as a gap | Slice 2.1 watched this repository live through a real daemon on Windows 10: 42 directories of 56,077 on disk, a save settled in 3.0 s against a 3 s window, 62 observations of one path collapsed to one pending change, deletion recorded, the pending list survived a hard kill, the reload recorded a fresh gap, and a reduced watch budget produced one capacity gap rather than a silent partial watch. Automated tests cover both platforms in CI | macOS behaviour under a real editing session, kqueue descriptor limits on a large repository, event storms from a branch checkout or a package install, and network or virtualized filesystems |

LCTK project stacks are Linux containers because [ADR-0011](adr/0011-zoekt-exact-search-backend.md) requires that boundary. LCTK probes the explicit managed Podman connection, requires a Linux server identity, and does not inspect or use an ambient Docker or Podman installation.

The initial resource goal is a 16 GB RAM, CPU-only machine. The measured million-file curves characterize one machine and synthetic corpora; they are not a latency guarantee or a supported project-count claim.

## Meaning of evidence

- **Build evidence** means source compiles for an environment.
- **Hosted test evidence** means automated behavior ran on the named GitHub runner image.
- **Artifact construction evidence** means an archive was created; it does not prove extraction or execution.
- **Local measured evidence** means a tracked harness drove a real third-party artifact on one maintainer machine and recorded the result. It is stronger than documentation and weaker than hosted test evidence, because it is not repeated automatically.
- **Certified** requires repeatable tests on the exact target contract, including the managed Podman/WSL runtime where relevant.

Current configurations are targets, not certified support claims. See [ADR-0008](adr/0008-platform-and-ci-baseline.md).

## Certification gates

| Gate | Status |
|---|---|
| Extracted-archive smoke on both target architectures | Complete on hosted Windows amd64 and macOS arm64; downloadable identity JSON and checksums were preserved by Stage 7. |
| Target-minimum operating-system execution | Windows 10 build 19045 executed the downloaded hosted archive and the local bootstrap/activation path on one maintainer machine; macOS 13 remains open. |
| Managed Podman/WSL mount, isolation, stop/start, and persistence | Real Windows 10 Podman 5.8.2 bootstrap, image/model self-test, repeated setup, and daemon restart are complete; clean-host creation on a user-selected second drive and concurrent project acceptance remain open. |
| Filesystem rename, coalescing, overflow, downtime, and reconciliation | Automated cross-platform tests and a real Windows editing session exist; a real macOS editing session remains open. |
| Installer/service, upgrade, rollback, and signing | The unsigned native setup completed a real partial-install recovery on Windows 10 build 19045, persisted both locations, registered Start menu/sign-in/uninstall, launched the installed daemon, and passed its loopback health check. Automated transaction tests now cover native setup install/upgrade/repair classification, downgrade refusal, reuse of the CLI update coordinator, same-version immutable repair, candidate project rollback, late setup failure rollback, previous-daemon restart, and stale-PID recovery after Windows reports access denied for an absent process. | A real `0.1.2` to `0.1.5` local-RC in-place upgrade, clean-host warning/reboot, relocation-before-create, explicit rollback, and complete uninstall acceptance remain open. |
| Baseline resource and stress measurements | Complete for the Stage 7 semantic and exact-search harness on one Windows/Docker Desktop machine; hardware breadth and concurrent-project stress remain open. |
