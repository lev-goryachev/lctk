# Compatibility

## Initial targets

### Host and runtime

| Component | Target contract | Current evidence | Not yet proven |
|---|---|---|---|
| Windows host | Windows 10 22H2 build 19045 or newer, amd64, with either working WSL2 or firmware virtualization available for WSL2 enablement | Go build and tests on `windows-latest`; host evaluation treats an operational WSL2 runtime as authoritative when active Hyper-V hides the firmware flag. On build 19045, the local unsigned native setup resumed a real partial installation, displayed and persisted both storage locations, completed bootstrap, created the Start-menu shortcut through `IShellLinkW`, registered sign-in startup and uninstall, launched the installed daemon, and returned `status: ok` from `127.0.0.1:4444/health`. Local RC upgrades continued through installed `0.1.11`: the setup closed only the running installed Admin executable that held the replacement target, activated the new core, restarted a healthy daemon, reopened the installed Admin window, and retained the managed runtime, project, indexes, and OAuth authorization. | A clean-host execution of the final unsigned setup through its Windows warning, WSL enablement and reboot, managed-machine creation, first project, and complete uninstall |
| macOS host | Development compatibility target only; no official package | Go build and tests on the native arm64 `macos-15` runner; the Stage 7 dry-run built, extracted, and executed the launcher/core archive and preserved its Darwin/arm64 identity JSON | An accepted runtime/distribution decision, execution on macOS 13, launch service, APFS edge cases, and end-to-end lifecycle evidence |
| Managed runtime | LCTK-private Podman `5.8.2`, explicit `lctk-runtime-root` connection, WSL2 machine `lctk-runtime` | Unit and transaction tests cover absolute client selection, explicit `XDG_DATA_HOME`, host-path translation, immutable downloads, safe and idempotent extraction, explicit network/volume/container lifecycle, runner isolation, and machine initialization arguments. A real Windows 10 run continued the managed machine after setup failure, pulled both signed OCI identities and the embedding model, passed the inference self-test, and repeated setup without replacing the running verified proxy. The local 0.1.12 acceptance installed only the signed `nvidia-container-toolkit-base-1.19.1-1.x86_64` RPM inside the private machine, exposed `nvidia.com/gpu=all`, and ran the digest-pinned CUDA image through the existing isolated project network. | Clean-host creation at a non-default runtime-data location, concurrent multi-project load, and runtime replacement/rollback |

### Client and capability

| Component | Target contract | Current evidence | Not yet proven |
|---|---|---|---|
| MCP | Streamable HTTP through official Go SDK v1.7.0 with OAuth 2.1 protected-resource discovery and project-bound bearer validation | In-process tests cover dispatch, scope, discovery, dynamic public-client registration, S256 PKCE, owner approval/denial, code exchange, refresh rotation, revocation, and cross-project refusal. An independent Go SDK client listed and called the complete 18-tool catalog against the persistent production stack, including expected typed refusals and memory recovery across restart, then repeated all 18 calls against the final Stage 7 image. | Other SDK versions, concurrent OAuth clients, and a complete authenticated 18-tool run through the installed OAuth surface |
| Codex client | Streamable HTTP, MCP protocol `2025-06-18`, OAuth discovery, dynamic public-client registration, S256 PKCE, and native owner approval | Before the OAuth migration, Slice 0.4 through Stage 7 proved Codex CLI handshake, route scope, reconnect, and all 18 tools through the superseded environment-token boundary; see [Stage 7 client verification](stage7-client-verification.md). On installed Windows RC `0.1.5`, Codex Desktop completed owner-approved authorization for `lctk-cqv5dg6m`. The same existing authorization survived upgrades through `0.1.11`, the GPU update to `0.1.12`, an explicit rollback to `0.1.11`, and the final `0.1.12` update. The final read-only `project_info` and `repository_map` calls required no configuration change, token copy, or IDE launch and reported healthy full-profile `0.1.12`, route-and-registry scope to `/workspace`, watcher pending zero, and fresh matching exact/semantic/graph generation 54 over 427 files. The active client rotated its own refresh state during these successful calls. | macOS, concurrent sessions, and a second long-lived client performing refresh across update and rollback |
| Second MCP client | Any client speaking Streamable HTTP and implementing the same OAuth public-client flow | Slice 1.4 historically proved route behavior through Claude Code using the removed environment-token boundary. The current OAuth flow is covered by protocol tests but has not yet been accepted through a second real client. | Dynamic registration, PKCE, owner approval, refresh, and tool calls through a second real OAuth client |
| Exact search | Persistent indexed literal and regular-expression search over the saved working tree | Slice 1.5 proved live indexing, mutation, restart reuse, and `git grep` parity. Stage 7 then published a real Docker-volume index from 1,000,000 ordinary bind-mounted files in 4,238.336 seconds and returned the unique literal match in 1,753.652 ms without truncation; see [stress evidence](stress.md). | Heterogeneous million-file repositories, concurrent query/index load, the million-file curve on arm64, and index behaviour across an engine upgrade |
| Semantic search | Persistent hybrid retrieval over AST-aware chunks, with one shared local embedding process and isolated per-project state | Stage 5 proved conceptual and exact-identifier queries through live MCP, persistence, batching, failure atomicity, and two projects sharing inference without sharing state. Stage 7 measured the production schema and exact-vector adapter through one million chunks with bounded ranking memory; see [stress evidence](stress.md). Installed `0.1.11` embedded 1,169 chunks over 356 files with CPU inference in 1,836 seconds. Installed `0.1.12` then rebuilt a changed, 2.197-times larger corpus of 2,568 chunks over 425 files with zero reuse on the GTX 1070 in 755.588 seconds. Telemetry peaked at 100% GPU utilization and 1,672 MiB; observed throughput increased from 0.637 to 3.399 chunks per second. The different corpora make this a machine characterization, not a controlled same-corpus benchmark. | Concurrent query/index load, heterogeneous million-file code, alternative vector adapters, additional GPU architectures, and macOS Docker Desktop |

Installed local `0.1.14` keeps the CPU `4096/4096` batch profile and uses `8192/8192` only for NVIDIA GPU inference. The immutable `swe-explore-sol-opus5-high-20-v6` one-instance gate indexed Astropy commit `19cc80471739bcb67b7e8099246b391c355023ee` into 20,622 semantic chunks on the GTX 1070. Its fsynced preparation series contains 288 samples: 147 measured at least 80% GPU utilization, with a 100% peak and an 87 C maximum. Exact, semantic, graph, and watcher state converged at generation 12 with zero pending paths. The 2,488.363-second settlement is unmeasured preparation time and is excluded from agent latency. This proves sustained CUDA execution and observable index progress on the acceptance machine; it is not yet mass-campaign performance evidence.
| Incremental indexing | A saved file reaches the search index without any command, and a search sees an edit made a moment earlier | Slice 2.2 ran the whole loop against this repository through a real daemon, a real container, and the live endpoint: a daemon start reconciled 172 files and closed its gap, a file written was found by search 0.2 s later against a 3 s debounce window, a deletion left the index one generation later, a removed directory took both of its files with it, and a file written while the daemon was down was picked up by the reconciliation on the next start | Bulk escalation on a real project large enough to reach the 500-file floor, sustained editing over hours, several projects updating at once, and behaviour when the index is slower than the edit rate |
| Command runner | Approved commands only, in a container with one writable mount, capped processes, memory and CPU, an explicit network policy, and a timeout | Slice 3.2 ran a real approved command in a real container against this repository and probed the guardrails from inside it: `pids.max` 512, `memory.max` 2 GiB, `cpu.max` 2 cores, `/workspace` writable and the only mount, no Docker socket, no name resolution under `none` and resolution under `full`. A command rewritten in the manifest was refused live and accepted again after re-approval; refusals and runs both reached the audit log | A command that ignores SIGTERM, very long builds, concurrent runs in one project, images without `/bin/sh`, and the re-download cost of having no cache mount |
| Git awareness | Branch, commit, dirty state, changed files, and a bounded diff, read-only and route-scoped | Slice 3.1 drove the tools live against this repository through the endpoint: all four tools listed, `project_info` carrying branch and commit, six changed paths reported with a deliberately wrong `project_id` argument, a 58-line single-file diff, a diff bounded to 80 bytes and reported truncated, and absolute, escaping, and dash-leading paths refused. The parser is tested against real repositories the tests create, on Git 2.54 | Older Git versions, worktrees and submodules, repositories on network or case-insensitive volumes, and very large working trees where status is slow |
| Filesystem watching | Native events on Windows and macOS, normalized to project-relative changes, with every lapse in observation recorded as a gap | Slice 2.1 watched this repository live through a real daemon on Windows 10: 42 directories of 56,077 on disk, a save settled in 3.0 s against a 3 s window, 62 observations of one path collapsed to one pending change, deletion recorded, the pending list survived a hard kill, the reload recorded a fresh gap, and a reduced watch budget produced one capacity gap rather than a silent partial watch. Automated tests cover both platforms in CI | macOS behaviour under a real editing session, kqueue descriptor limits on a large repository, event storms from a branch checkout or a package install, and network or virtualized filesystems |

LCTK project stacks are Linux containers because [ADR-0011](adr/0011-zoekt-exact-search-backend.md) requires that boundary. LCTK probes the explicit managed Podman connection, requires a Linux server identity, and does not inspect or use an ambient Docker or Podman installation.

The initial resource goal is a 16 GB RAM, CPU-only machine. The measured million-file curves characterize one machine and synthetic corpora; they are not a latency guarantee or a supported project-count claim.

## Meaning of evidence

- **Build evidence** means source compiles for an environment.
- **Hosted test evidence** means automated behavior ran on the named GitHub runner image.
- **Artifact construction evidence** means an archive was created; it does not prove extraction or execution.
- **Local measured evidence** means a tracked harness drove a real third-party artifact on one maintainer machine and recorded the result. It is stronger than documentation and weaker than hosted test evidence, because it is not repeated automatically.
- **Certified** requires repeatable tests on the exact target contract, including the managed Podman/WSL runtime where relevant.

Current configurations are targets, not certified support claims. See [ADR-0008](adr/0008-platform-and-ci-baseline.md).

## Local 0.1.12 NVIDIA GPU acceptance

The accepted unsigned setup and uninstaller are both 42,948,531 bytes. Their SHA-256 values are respectively `5e5e2b5a550732363fa6a251e9fb14ba03509de86d3f3c7bd9434e3e0fc7d9b6` and `0be582f239085377f76bf3f438c4604e9f05e20c5f531c9b4ea6c1321e4332c7`. They were built from commit `954da54e484ca87d50c32ef3f12b808c6a72c6fa`. Native update, explicit rollback, final update, and an offline `Download: 0 B` repeat plan preserved the installation directory, runtime-data directory, project, indexes, and owner-approved OAuth integration. Rollback ran the exact `0.1.11` core and CPU project stack; final update ran the exact `0.1.12` core and restored the persisted GPU selection.

The final runtime independently measured NVIDIA GeForce GTX 1070, driver `582.53`, 8,192 MiB VRAM, compute capability 6.1, pinned RPM `nvidia-container-toolkit-base-1.19.1-1.x86_64`, and CDI device `nvidia.com/gpu=all`. Podman image ID `1a8b5d7aeb67950d649c4c68dc7e8f70d7b81ab4070fdc7234350b5124ad40ef` matched signed digest `sha256:37dd122824e58af9ec861955242abdeeade5a1dcf0ad768bf2b37f903c2805c6` and measured 4,360,099,002 installed bytes. llama.cpp reported the physical adapter, a 66.92 MiB CUDA model buffer, a 200.16 MiB CUDA compute buffer, and complete `13/13` layer offload. Admin displayed `LCTK 0.1.12 | podman 5.8.2 linux | inference cuda NVIDIA GeForce GTX 1070 | Ready.`

The fresh full build ran from `2026-08-06T23:13:45.6187191Z` to `2026-08-06T23:26:21.2582770Z`. It embedded all 2,568 chunks from 425 files without semantic reuse and published generation 52 atomically in 755.588 seconds. Two concurrent user-file additions were then handled as ordinary watcher deltas; after settling, the existing OAuth client observed exact, semantic, and graph fresh at generation 54 over 427 files with zero pending paths.

## Certification gates

| Gate | Status |
|---|---|
| Extracted-archive smoke on both target architectures | Complete on hosted Windows amd64 and macOS arm64; downloadable identity JSON and checksums were preserved by Stage 7. |
| Target-minimum operating-system execution | Windows 10 build 19045 executed the downloaded hosted archive and the local bootstrap/activation path on one maintainer machine; macOS 13 remains open. |
| Managed Podman/WSL mount, isolation, stop/start, and persistence | Real Windows 10 Podman 5.8.2 bootstrap, image/model self-test, repeated setup, and daemon restart are complete; clean-host creation on a user-selected second drive and concurrent project acceptance remain open. |
| Filesystem rename, coalescing, overflow, downtime, and reconciliation | Automated cross-platform tests and a real Windows editing session exist; a real macOS editing session remains open. |
| Installer/service, upgrade, rollback, and signing | The unsigned native setup completed real in-place upgrades on Windows 10 build 19045 while retaining `C:\Users\Lev Goriachev\AppData\Local\lctk` and `D:\Programs\LCTK_data_store`. Acceptance through installed `0.1.12` exercised stale-daemon recovery, missing-image bootstrap, archive healthcheck preservation, signed post-load image identity, daemon synchronization, exact installed-Admin release, explicit rollback from GPU `0.1.12` to CPU `0.1.11`, and final update back to GPU `0.1.12`. The final setup activated core commit `954da54`, retained `0.1.11` as the verified previous version, restarted the matching daemon, reopened Admin, preserved the running project and existing OAuth authorization, and completed a fresh exact/semantic/graph index. Automated tests cover install/upgrade/repair classification, downgrade refusal, shared CLI/setup coordination, immutable same-version repair, candidate and late-failure rollback, daemon version synchronization, pre-0.1.12 CPU rollback, fail-closed stale-PID handling, installed-Admin release, and same-name process exclusion by exact path. Clean-host warning/reboot, relocation-before-create, same-version repair, and complete uninstall acceptance remain open. |
| Baseline resource and stress measurements | Complete for the Stage 7 semantic and exact-search harness on one Windows/Docker Desktop machine; hardware breadth and concurrent-project stress remain open. |
